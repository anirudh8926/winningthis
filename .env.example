# Environment variable template — copy to .env and fill in your values.
# .env is git-ignored; never commit real secrets.

# CORS: comma-separated list of allowed origins.
# Use "*" for local dev. In production, list your webapp's domain(s).
# Example: ALLOWED_ORIGINS=https://yourapp.vercel.app,https://yourapp.com
ALLOWED_ORIGINS=*

# MODEL_PATH: override path to credit_model.pkl (optional).
# Default: ./credit_model.pkl (relative to app/)
# MODEL_PATH=/absolute/path/to/credit_model.pkl

# Supabase (if your API reads from Supabase directly in future)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your-secret-service-role-key
